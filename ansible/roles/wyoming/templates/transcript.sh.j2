#!/bin/bash
python3 -c '
import sys, json, socket
text = sys.stdin.read().strip()
payload = json.dumps({"event": "transcript", "text": text}).encode("utf-8")
sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
sock.sendto(payload, ("127.0.0.1", 10701))
'
